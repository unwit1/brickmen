{
  "title": "Assembly normalization",
  "rules": [
    "assembly records reference component records",
    "do not count shortcut as unique mould",
    "preserve transform and component color",
    "patterned assembly may reference patterned components"
  ],
  "schema_version": "0.1"
}