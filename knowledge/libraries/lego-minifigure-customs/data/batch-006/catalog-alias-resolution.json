{
  "title": "Alias resolution",
  "rules": [
    "aliases are edges, not destructive renames",
    "moved-to targets retain old identifiers",
    "one source alias may map ambiguously until reconciled",
    "exact source namespace is mandatory"
  ],
  "schema_version": "0.1"
}