name: LEGO User Tracker Reconciliation

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/lego-user-tracker-reconciliation.yml'
      - '.github/lego-user-tracker-reconciliation-trigger'
      - 'tools/knowledge/reconcile_user_tracker_records.py'
      - 'knowledge/libraries/lego-minifigure-customs/data/user-collection/**'
      - 'knowledge/libraries/lego-minifigure-customs/data/user-design-targets/**'
      - 'knowledge/libraries/lego-minifigure-customs/data/compatible-brand-code-crosswalk-seed.json'
      - 'knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation/user-tracker-semantic-corrections-2026-09-26.json'
      - 'knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation/user-identity-alias-registry-2026-09-26.json'

permissions:
  contents: write

concurrency:
  group: lego-user-tracker-reconciliation-main
  cancel-in-progress: true

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out Brickmen
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Sync to latest main before generation
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git reset --hard origin/main

      - name: Build reconciliation layers
        shell: bash
        run: |
          set -euo pipefail
          out=/tmp/user-tracker-reconciliation
          mkdir -p "$out"
          python tools/knowledge/reconcile_user_tracker_records.py \
            --collection-dir knowledge/libraries/lego-minifigure-customs/data/user-collection \
            --design-dir knowledge/libraries/lego-minifigure-customs/data/user-design-targets \
            --crosswalk knowledge/libraries/lego-minifigure-customs/data/compatible-brand-code-crosswalk-seed.json \
            --semantic-corrections knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation/user-tracker-semantic-corrections-2026-09-26.json \
            --identity-aliases knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation/user-identity-alias-registry-2026-09-26.json \
            --output-dir "$out"
          dest=knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation
          mkdir -p "$dest"
          cp "$out/name-group-candidates.jsonl" "$dest/name-group-candidates-latest.jsonl"
          cp "$out/strict-duplicate-candidates.jsonl" "$dest/strict-duplicate-candidates-latest.jsonl"
          cp "$out/product-code-brand-candidates.jsonl" "$dest/product-code-brand-candidates-latest.jsonl"
          cp "$out/dc-legacy-normalized.jsonl" "$dest/dc-legacy-normalized-latest.jsonl"
          cp "$out/figure-release-candidates.jsonl" "$dest/figure-release-candidates-latest.jsonl"
          cp "$out/external-catalog-id-queue.jsonl" "$dest/external-catalog-id-queue-latest.jsonl"
          cp "$out/character-candidates.jsonl" "$dest/character-candidates-latest.jsonl"
          cp "$out/incarnation-candidates.jsonl" "$dest/incarnation-candidates-latest.jsonl"
          cp "$out/outfit-design-candidates.jsonl" "$dest/outfit-design-candidates-latest.jsonl"
          cp "$out/entity-resolution-review-queue.jsonl" "$dest/entity-resolution-review-queue-latest.jsonl"
          cp "$out/entity-resolution-review-summary.json" "$dest/entity-resolution-review-summary-latest.json"
          cp "$out/collection-gap-candidates.jsonl" "$dest/collection-gap-candidates-latest.jsonl"
          cp "$out/collection-gap-summary.json" "$dest/collection-gap-summary-latest.json"
          cp "$out/source-appearance-candidates.jsonl" "$dest/source-appearance-candidates-latest.jsonl"
          cp "$out/source-appearance-summary.json" "$dest/source-appearance-summary-latest.json"
          cp "$out/source-appearance-review-queue.jsonl" "$dest/source-appearance-review-queue-latest.jsonl"
          cp "$out/source-appearance-review-summary.json" "$dest/source-appearance-review-summary-latest.json"
          cp "$out/source-appearance-unresolved.jsonl" "$dest/source-appearance-unresolved-latest.jsonl"
          cp "$out/unresolved-code-prefixes.jsonl" "$dest/unresolved-code-prefixes-latest.jsonl"
          cp "$out/summary.json" "$dest/reconciliation-summary-latest.json"

      - name: Commit reconciliation snapshots
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "brickmen-ingestion-bot"
          git config user.email "brickmen-ingestion-bot@users.noreply.github.com"
          git add knowledge/libraries/lego-minifigure-customs/data/user-entity-reconciliation
          if git diff --cached --quiet; then
            echo "No generated changes."
            exit 0
          fi
          git commit -m "knowledge: refresh user tracker reconciliation snapshots"
          git push origin HEAD:main
